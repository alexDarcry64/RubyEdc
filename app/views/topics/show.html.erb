<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre:</strong>
  <%= @topic.nombre %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @topic.tag_id %>
</p>

<%= link_to 'Editar', edit_topic_path(@topic), class: "btn btn-warning" %> |
<%= link_to 'Regresar', tags_path, class: "btn btn-secondary" %>
